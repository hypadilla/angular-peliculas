import { IndiceCineDirective } from './indice-cine.directive';

describe('IndiceCineDirective', () => {
  it('should create an instance', () => {
    const directive = new IndiceCineDirective();
    expect(directive).toBeTruthy();
  });
});
