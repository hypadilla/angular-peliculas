import { EditarActoresDirective } from './editar-actores.directive';

describe('EditarActoresDirective', () => {
  it('should create an instance', () => {
    const directive = new EditarActoresDirective();
    expect(directive).toBeTruthy();
  });
});
