import { EditarGenerosDirective } from './editar-generos.directive';

describe('EditarGenerosDirective', () => {
  it('should create an instance', () => {
    const directive = new EditarGenerosDirective();
    expect(directive).toBeTruthy();
  });
});
